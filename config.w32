// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("fdatabase", "for fdatabase support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("fdatabase", "enable fdatabase support", "no");

if (PHP_FDATABASE != "no") {
	EXTENSION("fdatabase", "fdatabase.c");
}

